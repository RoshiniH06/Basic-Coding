document.addEventListener("DOMContentLoaded", () => {
  const button = document.getElementById("pinkBtn");

  button.addEventListener("click", () => {
    alert("Button clicked successfully!");
  });
});